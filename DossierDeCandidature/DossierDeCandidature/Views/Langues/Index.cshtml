@model IEnumerable<DossierDeCandidature.Models.Langues>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Langue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NiveauLangue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Autre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Niveau)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Langue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NiveauLangue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Autre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Niveau)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
